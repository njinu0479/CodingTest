#include <iostream>

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n;
    cin >> n;

    if (n == 0) {
        cout << "YONSEI";
    } else {
        cout << "Leading the Way to the Future";
    }

    return 0;
}